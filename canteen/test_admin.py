import os, django; os.environ.setdefault("DJANGO_SETTINGS_MODULE", "canteen.settings"); django.setup(); from django.test import Client; from accounts.models import CustomUser; usr = CustomUser.objects.filter(is_superuser=True).first(); c = Client(); c.force_login(usr); r = c.get("/admin/accounts/order/"); print("STATUS:", r.status_code); print("CONTEXT STATS:", r.context.get("stats") if r.context else "NO CONTEXT"); print("IN HTML:", b"Under ₹100" in r.content)
